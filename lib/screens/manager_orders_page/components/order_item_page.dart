import 'package:flutter/material.dart';

class OrderItemPage extends StatelessWidget {
  const OrderItemPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}